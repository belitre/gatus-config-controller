apiVersion: v2
name: gatus-config-controller
description: Kubernetes controller that generates Gatus endpoint config from Ingress and HTTPRoute resources
type: application
version: 0.1.0
appVersion: "canary"
dependencies:
  - name: gatus
    version: "1.5.0"
    repository: "https://twin.github.io/helm-charts"
